Leia o CLAUDE.md e implemente o Sprint 1 completo (Estoque -> Cardapio), seguindo a ordem descrita la: primeiro backend/app/database.py, depois backend/app/models.py (as 6 entidades do Sprint 1), backend/app/schemas.py, o service backend/app/services/disponibilidade.py, e por fim os routers ingredientes.py, estoque.py, cardapio.py e auth.py. Registre os routers em backend/app/main.py. Nao implemente nada do Sprint 2 (pedidos, mesa, carrinho) ainda. Use SQLite como banco (a DATABASE_URL ja esta configurada no .env para isso) e crie as tabelas com Base.metadata.create_all() direto, sem migracao formal por enquanto. No final, confirme que uvicorn app.main:app --reload sobe sem erro e que /docs mostra os endpoints.